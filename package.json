{
  "name": "enclave-sign-api",
  "private": true,
  "scripts": {
    "vendor": "node scripts/vendor-sign-sdk.mjs",
    "predeploy": "npm run vendor",
    "deploy:db": "npx supabase db push",
    "deploy:functions": "npm run vendor && supabase functions deploy exchange-account-token sign-open-envelope sign-complete-recipient sign-send-invite sign-download-completed --project-ref dqjwchquqeznftdnncec --import-map ./supabase/functions/deno.json",
    "deploy": "npm run deploy:db && npm run deploy:functions"
  }
}
